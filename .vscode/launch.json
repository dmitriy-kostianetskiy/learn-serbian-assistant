{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node-terminal",
      "request": "launch",
      "name": "Lanch",
      "command": "npm run bot",
      "cwd": "${workspaceFolder}/functions"
    }
  ]
}
